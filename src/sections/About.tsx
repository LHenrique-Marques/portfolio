import SectionTitle from "../../src/components/SectionTitle";
const About = () => {
  return (
    <SectionTitle className="p-8">
      <h2 className="text-2xl font-bold">Sobre mim</h2>
      <p>Sou desenvolvedor focado em React, NestJS, etc...</p>
    </SectionTitle>
  );
};

export default About;